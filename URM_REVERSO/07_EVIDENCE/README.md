# Evidências

O trabalho de análise contém 18 capturas do URM em runtime. Entre as evidências já identificadas: Choices em `0018`/`0019`; Path Detection em `0017`; VarsStore em `0003`; `archivePath` em `0015`/`0016`; changelog URM 2.6.2 em `0012`.

As capturas devem ser tratadas como evidência de comportamento/runtime, não apenas como ilustração.

A fonte técnica de análise é `MOD_ANALISE.zip`. Os `.rpy` convertidos do pacote são artefatos produzidos durante o processo de análise, e não devem ser confundidos com o código-fonte originalmente distribuído.
